#!/bin/bash

# Remove all tmux sessions
sessions=$(tmux ls -F '#S' 2>/dev/null)
if [ -n "$sessions" ]; then
    for s in $sessions; do
        tmux kill-session -t "$s"
    done
    echo "All tmux sessions removed."
else
    echo "No tmux sessions found."
fi

